import 'package:flutter/material.dart';

class TopUpWalletPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Top Up Wallet'),
      ),
      body: Center(
        child: Text('Top Up Wallet Page Content Here'),
      ),
    );
  }
}

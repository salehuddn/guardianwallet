import 'package:flutter/material.dart';

class TransferFundDependentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Transfer Fund to Dependent'),
      ),
      body: Center(
        child: Text('Transfer Fund Page Content Here'),
      ),
    );
  }
}
